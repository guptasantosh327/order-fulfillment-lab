node_modules/
dist/
*.tsbuildinfo
.env
.env.*
.DS_Store
.idea

# Terraform — local state + provider cache never get committed; the lock file does.
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan